---
:dataminer_definition: location_assignments
:multiselect:
  :locations:
    :url: "/masterfiles/locations/locations/$:id$/link_assignments"
    :preselect: "SELECT location_assignment_id AS id FROM location_assignments_locations WHERE location_id = $:id$"
    :section_caption: "SELECT 'Location Assignments for ' || location_long_code FROM locations WHERE id = $:id$"
    :multiselect_save_method: remote
