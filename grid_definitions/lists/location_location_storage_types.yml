---
:dataminer_definition: location_storage_types
:multiselect:
  :locations:
    :url: "/masterfiles/locations/locations/$:id$/link_storage_types"
    :preselect: "SELECT location_storage_type_id AS id FROM location_storage_types_locations WHERE location_id = $:id$"
    :section_caption: "SELECT 'Location Storage types for ' || location_long_code FROM locations WHERE id = $:id$"
    :multiselect_save_method: remote
